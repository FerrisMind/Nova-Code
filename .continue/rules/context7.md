---
description: Use this rule when editing or adding UI code for the Tauri + Svelte
  project to ensure a consistent, VS Code-like layout.
alwaysApply: true
---

When building the VS Code/Cursor-like UI, structure the app into clear layout components (Titlebar, ActivityBar, Sidebar/Explorer, EditorArea with tabs, StatusBar, CommandPalette) with realistic styling and no backend/command invocations.